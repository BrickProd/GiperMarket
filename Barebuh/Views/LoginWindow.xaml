<Window x:Class="Barebuh.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Barebuh.Views"
        mc:Ignorable="d"
        Title="Окно входа в баребух" Height="300" Width="400" WindowStartupLocation="CenterScreen">
    <Grid>
        <TextBox x:Name="textBoxLogin" HorizontalAlignment="Left" Height="23" Margin="79,52,0,0" TextWrapping="Wrap" Text="{Binding Username}" VerticalAlignment="Top" Width="120"/>
        <PasswordBox x:Name="passwordBox" HorizontalAlignment="Left" Margin="79,116,0,0" VerticalAlignment="Top" Width="152"/>
        <Button x:Name="buttonLogin" Content="Button" HorizontalAlignment="Left" Margin="69,202,0,0" VerticalAlignment="Top" Width="75" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=passwordBox}"/>
        <TextBlock x:Name="textBlock" HorizontalAlignment="Left" Margin="79,161,0,0" TextWrapping="Wrap" Text="{Binding Status}" VerticalAlignment="Top" Width="152"/>


    </Grid>
</Window>
